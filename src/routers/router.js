import express from 'express'
// import { listar, puxarId, criar, atualizar, excluir} from '../controller/cerveja.js'

const router = express.Router()

// router.get('/', listar)
// router.get('/:id', puxarId)
// router.post('/', criar)
// router.put('/:id', atualizar)
// router.delete('/:id', excluir)


export { router }